# AlphaZero Capital — Requirements (v4.0)
# Install: pip install -r requirements.txt

# ── Core ─────────────────────────────────────────────────────────────────────
python-dotenv>=1.0.0
requests>=2.31.0
numpy>=1.24.0
pandas>=2.0.0
scipy>=1.11.0

# ── Market Data ───────────────────────────────────────────────────────────────
yfinance>=0.2.38             # Yahoo Finance (free, always available)
nsepython>=2.0               # NSE India direct API wrapper
finnhub-python>=2.4.19       # Finnhub SDK (free 60 req/min)
twelve-data>=1.2.14          # Twelve Data SDK (free 55 req/min)
# alpha_vantage is accessed via raw requests (no SDK needed)

# Upstox SDK (for native Upstox API — free, no monthly charge)
upstox-python-sdk>=2.0.0

# ── Technical Indicators ──────────────────────────────────────────────────────
pandas-ta>=0.3.14b           # Pure-Python TA library (no TA-Lib C dependency)
# ta-lib                     # Optional: faster C-based library if installed

# ── AI / LLM ─────────────────────────────────────────────────────────────────
anthropic>=0.29.0            # Claude API
openai>=1.30.0               # GPT-4 optional fallback
google-generativeai>=0.7.0   # Gemini optional fallback
# openrouter is accessed via raw requests usually

# ── News / NLP ────────────────────────────────────────────────────────────────
transformers>=4.40.0         # HuggingFace (for FinBERT sentiment)
torch>=2.1.0                 # PyTorch backend for transformers
sentencepiece>=0.2.0         # Tokenizer support

# ── RL (KARMA Agent) ─────────────────────────────────────────────────────────
stable-baselines3>=2.3.0     # PPO for KARMA learning agent
gymnasium>=0.29.0            # RL environment API
shimmy>=1.3.0                # Gym/Gymnasium compatibility shim
optuna>=3.6.0                # Bayesian optimization for params

# ── Backend / Dashboard ───────────────────────────────────────────────────────
fastapi>=0.109.0
uvicorn>=0.27.0
pyarrow>=15.0.0              # For parquet support (NEXUS/LENS)
xgboost>=2.0.0               # For NEXUS regime detection
scikit-learn>=1.4.0          # For ML utilities

# ── Telegram ──────────────────────────────────────────────────────────────────
python-telegram-bot>=21.0    # Telegram alerts

# ── Utilities ─────────────────────────────────────────────────────────────────
schedule>=1.2.0              # Job scheduling
pytz>=2024.1                 # Timezone handling
tqdm>=4.66.0                 # Progress bars
colorlog>=6.8.0              # Colored logging output

